
import {
    createStore,
    applyMiddleware,
    combineReducers,
    AnyAction,
    Store,
} from "redux";

export interface IState {

}
const reducer = (state: IState | undefined, action: AnyAction) => {

}